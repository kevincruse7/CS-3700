package cs3700.project5.controller.crawler;

import java.io.Closeable;

/**
 * Represents a web crawler application that searches a website for flags.
 */
public interface WebCrawler extends Runnable, Closeable {
}
